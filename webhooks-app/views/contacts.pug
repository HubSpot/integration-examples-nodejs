extends includes/layout
block content
  .container
    pre #{searchDetails}
    table
      thead
        tr
          th ContactID
          th Name
          th Company

      tbody
        each contact in contacts
          tr
            td #{contact.id}
            td #{contact.name}
            td
              each hookEvent in contact.events
                span(class='hook') #{hookEvent.event_type}
